<Page x:Class="Revive.Pages.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Black">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="REVIVE" FontSize="42" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,36"/>

            <TextBlock Text="Build" Margin="0,0,0,4"/>
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" x:Name="BuildBox" Height="32" DisplayMemberPath="Name"/>
                <Button Grid.Column="1" Content="+" Width="34" Height="32" Margin="8,0,0,0" Click="Add_Click"/>
                <Button Grid.Column="2" Content="-" Width="34" Height="32" Margin="6,0,0,0" Click="Remove_Click"/>
            </Grid>

            <Button Content="Play" Height="50" FontSize="16" Click="Play_Click"/>
        </StackPanel>
    </Grid>
</Page>
